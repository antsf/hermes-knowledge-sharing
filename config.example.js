// LOCAL DEV ONLY — this file is gitignored and never committed.
// In production (GitHub Pages), config.js is generated at deploy time by
// .github/workflows/deploy.yml, injecting HERMES_FORM_TOKEN from repo secrets
// — the real token never touches git history.
window.HERMES_FORM_TOKEN = "";
